# Board Race 开发交接

状态：`complete / live-playtest-next`

更新时间：2026-08-19

## 当前工作包

- Base：`77320ed6eb86d0db04673be43a978a7ba288d5b2`，`main`。
- 目标：只重做“强风外海”海面；让真实波形而非颜色或高亮承担汹涌感。
- 代码 owner：`src/water/waves.ts`、`src/water/ocean.ts`。船体、角色、尾流、水花、天空、
  相机、路线和 HUD 均未修改。

## 本轮完成

- 波形改为五向二阶高度场；CPU 浮力、解析法线和 GPU 位移使用同一相位与公式，删除旧的
  GPU 横移、CPU 只采高度的近似。
- 近场保留 128/48/27/15/8.5m 五档波，200-360m 连续收掉短波，远场保留主涌浪轮廓，
  避免稀疏地平线网格混叠。
- 海面仍是一张 opaque draw；物理高度、坡度、垂直速度和曲率共同决定明暗、反射和白帽。
  删除脱离物理波形的宽风法线与大面积绘制式高光，白帽增加双尺度断裂。
- 高质量档只增加近场细 glint；性能档编译掉该层，不改变波形、浮力或碰撞。

## 机器与截图证据

- `npm run build`：通过；生产 JS 1,048.73 kB / gzip 289.16 kB，旧包为 1,051.34 kB。
- `npm run verify:smoke`：通过；桌面 174 calls / 325,529 triangles，手机 194 calls /
  328,545 triangles，均报告 16.7ms 固定 harness 帧。
- `npm run verify:collision`：通过，15 组船体配对和飞行门隔离保持有限值。
- 起跑 before -> after：桌面 334 -> 309 calls、348,299 -> 336,507 triangles；手机同为
  334 -> 309 calls、348,299 -> 336,507 triangles。绘制像素分别保持 2,025,000 与 2,057,250。
- 静态截图：`shots/ocean-open-sea/{before,after}/`，各含桌面与 `844x390` 的 start、
  drift-charge。`shots/ocean-open-sea/motion/` 保留三个连续时点供人工检查波峰传播；相机和船
  也在推进，因此不把全帧像素差冒充海浪质量证据。
- 人工检查只确认：地平线和船下表面出现多尺度真实起伏，白帽跟随合格物理波峰且不再形成
  连续长带，未出现全屏绿色偏色。是否达到商业审美仍以用户线上试玩为准。

## 遗留风险

- 证据来自无头 Chrome/软件 WebGL；真实手机 GPU、Pages 线上观感和长局可玩性尚未人工验证。
- 强浪会明显改变船体俯仰和起跑编队画面；碰撞门禁通过，但这不等于所有比赛节奏已试玩。
- 起跑左侧仍能看到既有的宽冷色反光区；本轮已压低权重，是否继续保留需结合真机动态判断。
- 尾流、水花、开场落座、船体模型、16 骨骼角色和播报美术均未改，不能写成已改善。

## 下一工作包队列

1. 先试玩本次海面。若方向不通过，只在两个海面 owner 文件内继续，不把其他美术混进来。
2. 移动端聚焦反馈：把漂移/飞行储备小组件改成与 PC 同类的船侧悬浮布局，优先船左侧并做
   屏幕边缘避让；续飞、过门成就和通知整体适度上移，但保持在主视线附近且不覆盖船体。
3. 新手规则与电台：先核对“3 格库存但只能用两个”指库存还是单次飞行的起飞+一次续飞，
   再选择最轻的一次性教学；调查 Gemini 被中断后恢复为何像重复播报，改成持久化的一次性、
   可读完且不每局复读的真人化内容，不能用堆更多固定台词解决。
4. 移动端边缘速度/受光线条减细，保留偶发阳光掠过和运动感；PC 只在对比后决定是否同步。
5. 上述反馈包稳定后，再分别处理尾流/水花/开场落座与船体/角色，继续保持一包一主题。
