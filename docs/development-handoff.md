# Board Race 开发交接

状态：已完成 8 对水面 checkpoint 浮标的空间互斥与坐标纠偏，等待用户在线复核。

## 当前活动工作包

- 基线：`75bdb85`。
- checkpoint 浮标精确坐标纠偏：使用 Catmull-Rom 曲线解析采样出的严格纯水面航道坐标修正 `GATE_ANCHORS`，确保 8 对浮标严格落在 7 段纯水面航段中（涵盖第 4 飞落水后发夹弯的 CP5），彻底消除第 5 飞空中雾道下方的浮标残留。
- 飞行参数解耦：解耦 `FLIGHT_ROUTES[1]` 对 `CHECKPOINT_US` 的常数依赖，航线与检查点完全独立。
- 浮标朝向校准：在 `buildGates()` 中统一设置浮标沿赛道切线法向的 yaw 旋转，修复急弯处摆放歪斜问题。

## 验证

- `npm run build`：通过。
- `npm run verify:smoke`：桌面 `1440x900` 与手机 `844x390` 通过。
- `npm run verify:collision`：通过，浮标撞击减速、羽毛爆炸、落水与归位生命周期完全正常。
- `npm run verify:audio`：通过，碰撞音效与电台优先级契约正常。
- 桌面与 `844x390` 实拍复核：各航段空门正下方海面完全干净，水面浮标朝向端正。

## 唯一下一步

- 用户在线实机复核水面检查点与空门空间互斥效果。
