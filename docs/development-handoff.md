# Board Race 开发交接

状态：本文件所在版本交付标志性灯塔、可读主动尾翼，并合入鸭子气球浮标喜剧效果。

更新时间：2026-08-23

## 当前活动工作包

- 基线：`ef70ab0`；已合并 `feat/balloon-buoy-comedy`。
- 新增固定纯视觉 `LighthouseLandmark`：位于 `(110, 0, 190)`，不接入碰撞、路线或 AI；暖黄扫光按模拟时间旋转。
- 修复尾翼动态可读性：从上一帧 `state.steer` 改为当前固定步转向输入；漂移和飞行空刹采用不同抬升量，左右转向产生相反差动，松手以 `omega=11.5`、`zeta=0.62` 回落。
- 传送和重开会清零两片翼面的角度、角速度和目标，不继承上一段动作。
- harness 新增灯塔资产/追逐镜头、左右漂移、左右空中刹车镜头，以及真实输入的部署、转向反转、释放速度和稳定状态断言。
- checkpoint 浮标升级为海事标杆与鸭子气球：撞击扣除 20% 速度，浮标翻飞，气球旋转升空后爆成池化羽毛；重开和归位会完整恢复气球。
- 气球爆开的声音和触觉只归属于撞击它的玩家，AI 撞击不会震动玩家设备。

## 实际验证

- `npm run build`：合并后通过。
- `npm run verify:smoke`：合并后桌面 `1440x900` 与手机 `844x390` 全部通过。
- `npm run verify:collision`：合并后通过，浮标抛物、落水和归位生命周期保持有效。
- 美术总监按桌面与手机最终实拍复核：灯塔剪影、色块、暖黄扫光和追逐可读性通过；漂移与空中刹车的左右差动通过。
- 灯塔单项相对 `ef70ab0` 的 `race-straight`：桌面 `+0 calls / +0 triangles`；手机宽视野 `+3 calls / +5052 triangles`。

## Pending 与风险

- 代码侧无 pending。
- 剩余验证是用户在线实机手感复核；尤其观察漂移、空中按住空刹转向，以及松手后的阻尼回落是否符合主观节奏。

## 唯一下一步

- 用户在发布版本实机复核尾翼动态；如仍不够可读，记录设备、动作阶段和左右转向方向，再以该场景定点调整，不回退到静态截图验收。
