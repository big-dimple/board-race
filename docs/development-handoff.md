# Board Race 开发交接

状态：`wave spectrum proto 2 + rotating sparkle stars / 待真机确认`

更新时间：2026-08-22

## 已完成

- 波形原型 2(`waves.ts`,用户授权):48/27/15m 能带各拆 ±15-20° 双分量 → 短峰海,
  峰线断成几十米分段(条纹根治);crest 谐波 0.2-0.34 尖峰宽谷;8.5m 增至 0.14
  船身尺度碎浪。相位实现经全表扫描选定:起步窗口零接触(旧海靠 Y 闸遮蔽接触,
  新海暴露后需要选对实现)。
- 闪点旋转四芒星(ocean.ts):星芒随时间慢转(每 cell 方向/速率按 hash 0.3-1.0 rad/s),
  随闪动绽放;星芒入门从 18m 放到 6m,近处是小星不是圆点。
- 冒烟/harness 修复(均为时间线免疫化,不断言时机只断言合同):
  - `stageRadioTechniqueBroadcast`:固定 4.2s 采样改为推进到广播真正呈现(广播不出现
    或比赛结束仍会失败)。
  - 电台用例去重断言从"队列长度"改为按 key 判定(enqueue 返回值/radioHas);
    RadioDirector 加 hasKey,RaceTower 加 radioHas,announceTechniqueTip 返回是否入队。
  - 广播卡测量等待"真正呈现在视口内"(动画是 5.65s 滑入/停留/滑出全生命周期,
    固定时刻采样必踩随机相位)。

## 排障记录(为什么这轮久)

新海让起步车阵接触从"被 Y 闸遮蔽"变成真实发生:蹭碰推偏玩家→吃到补给包→起飞提示
提前弹出→按设计优先级压住开场广播→冒烟固定时刻采样必挂。另发现广播动画是全生命周期
(含滑出),fixed-settle 测量必踩随机相位。

## 实际验证

- `npm run build` 通过;`verify:smoke` 连续两次 OK(桌面+844x390);`verify:collision` OK。
- 截图自审:`shots/v8`(ocean-near/sunpath/start/countdown)短峰碎浪与尖峰成立。
- 起步接触、丁达尔呼吸、旋转星芒为动态项,**待真机确认**。

## 下一步

- 空中雾道可读性(已诊断,未动):雾带太细太低对比,读成拖尾不像"航道";缺方向感与
  下一门/落点指引。候选:加粗边缘+流动方向脉冲+门标塔提亮,先出一屏原型再扩展。
- 真机反馈后调:heave 强度、星芒转速、丁达尔呼吸频率。
