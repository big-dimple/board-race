# Board Race 开发交接

状态：`sun/sky/sparkle polish / 待真机确认`

更新时间：2026-08-22

## 已完成

- 前两批已发布:`5071367` 海面体积感+低日套装,`5924691` 飞行提示卡桌面修复。
- 真机反馈四条,本轮处理(`sky.ts`/`ocean.ts`):
  - 太阳光晕十字线(fourPoint/horizontalFlare)删除——参考图没有的卡通线。
  - 丁达尔光束不再像贴纸:三束各自慢时钟强度游移(0.55±0.45,周期 23-48s),
    整体 veilBreathe 0.7±0.3;`uTime` 接入天穹 shader。
  - 倒计时闪点收敛:近景八度核心 ×0.6、密度近半、uSparkleTwinkle 1.0→0.55、
    每 cell 闪形 pow 指数 1.6-3.2 按 hash 变化(不再单一爆闪曲线),去掉
    uOpeningArt 对闪点的加成;行驶中的远场光路未动。
  - 水面平行条纹:风纹法线加漂移斑块调制(0.72-1.0,地板不能更低——归零的
    斑块在掠射角变成镜面大白斑,v6 已教训)。

## 实际验证

- `npm run build` 通过;截图自审:`shots/v7` countdown(近景闪点收敛成立)、
  ocean-sunpath(十字线消失、光束在、海面与你已接受的 5071367 一致)。
- 中途 v6 出现过镜面白斑回归,已定位为 ripplePatch 地板过低+闪形指数过低,修复后消失。
- 丁达尔呼吸是时间项,静帧无法验证效果,**待真机动态确认**。

## 下一步

- 真机看:丁达尔呼吸节奏(快了/慢了调 0.21/0.147/0.27 三档频率)、倒计时闪点、条纹观感。
- 条纹的根治在波形原型 2(短峰性,crest 谐波+浪群包络,改 `waves.ts`,需 `verify:collision`)。
