# Board Race 开发交接

状态：队伍协作三站试玩已完成代码闭环，等待实体双人线上体感反馈。

## 当前活动工作包

- 基线包含资料片手柄修正提交 `f18c538`；本工作包将旧七阶段尾流玩法替换为驾驶校准、共振接力、
  双锁水闸和水空协奏。
- 队伍协作使用手动前进 / 制动倒车；独立竞技自动油门、`BoatInput` 和 60 Hz fixed-step 不变。
- 门控手移动的飞行门共用模型与 swept 判定中心；未供能不消费起飞格，飞行失败回到起飞前。
- 队伍选角使用紧凑完整立绘，队伍追拍使用远距配置；独立竞技、移动端和游戏内小头像不变。
- 存档升级到 `board-race:team-expedition:v2`；旧版仅迁移选手，教学完成后可跳过或主动重玩。

## 已验证

- `npm run build`：通过。
- `npm run verify:smoke`：桌面与 `844x390` 横屏启动、渲染和 HUD 契约通过。
- `npm run verify:collision`：共享 world transform 与碰撞诊断通过。
- `npm run verify:team`：真实键区完成教学与三站；覆盖输入隔离、漏接重试、门锁交换、门控飞行、
  低速双泊位、v2 存档和交换职责重玩。
- 桌面截图：玩法目录、紧凑选角和 50/50 远距分屏已人工复核；两侧画面非空。

## 遗留实机项

- 自动化覆盖标准 Gamepad API，但仍需两只实体手柄确认 RT/LT 行程、X 持续能力、Start 暂停和震动。
- 首次朋友试玩重点记录：第一处看不懂的位置、是否有人等待过久、飞行门沟通是否自然、三站后最想重玩的职责。

## 唯一下一步

- 发布后用一套键盘或两只手柄完成一次无调试三站试玩，根据双人反馈只调提示时机、目标距离和容错，
  不退回尾流核心循环。
