# Board Race 开发交接

状态：`tilt steering retune / 已发布,待真机手感确认`

更新时间：2026-08-20

## 已完成

- 勋章仪式停留 4.5s→6.5s(`MEDAL_CEREMONY_S`),"继续游戏"按钮提前到 4.5s 出现
  (`MEDAL_MIN_READ_S`),给资料片入口留出真实点击窗口;打开画廊仍暂停计时。
- 重力转向按商业赛车手游方式重调(`src/core/mobileControls.ts` read()):
  - 自适应滤波:稳手保持 0.11s 防抖;误差 >~9°(猛打/换向)时滤波放到 ~0.025s,
    换向几帧内响应。原先固定 τ=0.12s,换向拖 0.2~0.3s。
  - 死区 3°→2°;满舵 22°→14°(爬坡 (deg-2)/12)。
  - 触控按钮与键盘的 7~8/s 爬坡未动(用户反馈这两者没问题)。
- 上一工作包(三门勋章仪式资料片入口、删勋章截图)已随 `88b768b` 发布;
  harness 电台检查计时竞态(160ms→700ms)同批修复。

## 实际验证

- `npm run build` 通过;`npm run verify:smoke` OK(桌面+844x390)。
- 滤波/死区参数为手感改动,harness 无法验证手感,**待用户真机确认**;
  不合适就继续调 tiltTau/swing 斜率或满舵角。

## 下一步

- 用户真机试重力转向;若仍不满意,候选:满舵角再降、加灵敏度档位、或隐藏重力入口。
