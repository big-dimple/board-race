# Board Race 开发交接

状态：`pending / next-session`

更新时间：2026-08-20

## 下一工作包

- Base：`main` 当前 HEAD；本次海面光照收尾前的已知父提交为
  `b7a79aea7a0590509a0d77c2350392565faa034e`。
- 目标：修正桌面/移动反馈布局并降低电台重复感。先解决视线与遮挡，再处理一次性教学；
  不改海面、船体、角色、路线、飞行物理或三格库存上限。
- 本文是下一窗口的任务输入；HUD、电台和下列玩法事项均未在本次海面收尾中实现。

## 当前已完成基线

- 用户已验收强浪幅度。本次仅修改 `src/water/ocean.ts`：移除跟随长涌浪缓慢移动的宽白高光，
  改为连续短波驱动的快速稀疏水闪；`src/water/waves.ts`、浮力与碰撞未改。
- 桌面与 `844x390` before/after 在 `shots/ocean-glint-speed/{before,after}/`，连续 0.2 秒帧在
  `shots/ocean-glint-speed/motion/`。`npm run build`、`npm run verify:smoke` 已通过。

## 用户最新决定

1. **PC 二次补飞必须恢复原位。** 当前 `flight-extend` 被全平台统一丢到右上角，这是回归。
   桌面恢复到 `77320ed` 之前靠近主视线、位于船体上方的居中表达；不要套用手机规则。
2. **移动端也不能把续飞提示扔远。** 保持在主视线附近，只做适度垂直上移，使文字块不覆盖
   玩家船体和紧急航线；不要固定到右上角。桌面与移动端必须使用明确分开的布局规则。
3. 横屏手机的漂移/飞行储备菱形组件当前右侧位置可以保留，默认不改。若 after 截图仍显得离主视线
   太远，只在船右侧适度拉近，同时避开车手、屏幕边缘和右侧触控区；不要移到船左侧挤压排名区，
   也不能回到角色头顶。
4. 续飞、过门成就和其他短通知都要检查是否覆盖玩家船体。移动端统一原则是“略向上、仍看得见”，
   但不能用一个粗暴全局偏移处理所有通知；按实际尺寸和优先级分别落位。
5. 移动端两侧偶发的速度/受光线条保留，但应明显减细；PC 当前粗细可先不改。

## 规则与电台

- 真实规则是最多储备 3 格；起飞消耗 1 格，同一次飞行最多再消耗 1 格续航。因此新手需要理解
  “单次飞行最多两次消耗（起飞 + 一次续航）”，不是把库存上限改回 2。先选择最轻、一次性的
  教学时机，不能增加常驻说明或挡住比赛。
- Gemini 技巧广播目前虽有页面会话去重，但被高优先级内容打断/恢复时仍可能被玩家感知为多次，
  且每次打开页面都播同一句会审美疲劳。下一窗口先查清真实触发、排队和恢复路径，再改成一次
  有意义且能读完的出现；是否跨页面持久化由现有存档模型决定，不能靠堆更多固定台词制造“人味”。

## 后续玩法队列

- **Pending：偏离主线判负宽限改为 15 秒。** 当前第三关急转弯若未及时空刹，会过早触发偏航判负。
  单独定位真实 off-course 计时 owner，修改实际判负时长并补一个聚焦行为检查；不要把它与撞柱、
  飞行门或路线走廊失败混为一类，也不要顺手塞进本 HUD 工作包。

## 实施边界

- 开始前先截 `flight-extension-spool`、`flight-ready` 和包含过门通知的桌面/`844x390` 基线，
  用截图确定船体、航线、触控区和文案矩形，不凭 CSS 数字猜位置。
- 优先检查 `src/hud/hud.css`、`src/hud/hud.ts`、电台 director 与现有存档 owner；只修改本工作包
  实际需要的文件。不要顺手改海面或开启船体/角色重做。
- 更新 smoke 时只保护真实结果：桌面续飞在主视线附近且不在右上角；手机续飞不遮船/航线且
  不离开主视线；船侧组件不遮车手和触控区。不要增加固定像素快照或新大型 harness。
- 最小验证：`npm run build`、`npm run verify:smoke`、桌面与 `844x390` after 截图。电台逻辑若改，
  再按需运行 `npm run verify:audio`；不相关的碰撞和长跑保持 pending。

## 完成标准与后续

- 只有桌面和手机截图都人工确认“看得到但不挡船/航线”，才能写布局完成；机器门禁不代替审美。
- 完成后覆盖本文为实际改动、验证、遗留风险和唯一下一步，并保持 `llmwiki` 只记录最终稳定合同。
- 本工作包之后再分别处理尾流/水花/开场落座，以及船体/16 骨骼角色；不要混入本轮。
