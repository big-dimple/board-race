# Board Race 开发交接

状态：已完成直线巡航水花与舷侧破浪水沫效果优化，等待用户在线复核。

## 当前活动工作包

- 基线：`201c4ff`。
- 直线巡航破浪水花：在 `ISpray` 与 `SpraySystem` 中新增真实流体动力学定向舷侧水花 `chine()`，在水面直行巡航速度大于 10 m/s 时从船身中后部左右舷交替激起细腻短促的破浪水沫。
- 转向与破浪水花升级：转向破浪水花同步升级为向舷外、向上并继承前进惯性的定向水花粒子，告别死板的径向爆开。
- 性能与确定性：水花粒子完全消费预分配的 instanced 粒子池与定长 typed-array，固定步物理循环 0 额外内存分配。

## 验证

- `npm run build`：通过。
- `npm run verify:smoke`：桌面 `1440x900` 与手机 `844x390` 通过。
- `npm run verify:collision`：通过。
- `npm run verify:audio`：通过。
- 桌面与 `844x390` 实拍复核：直线巡航有适度自然的水花飞溅，不遮挡主视线与对手特效。

## 唯一下一步

- 用户在线实机复核直线巡航水花与视觉手感。
